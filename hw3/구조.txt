학생 정보가 담긴 파일은 미리 생성, c프로그램에서는 가져오기만.


MAIN

구조체 정의 

무한루프 case 문 (case 1,2,3)

1. insert 선택 시

	파일을 불러오고, FILE *fp = fopen("c:\\자료구조\\파일명.txt", "읽기쓰기type");
		

	파일 내용을 구조체에 저장( 저장 후 fclose(fp);   // 파일 포인터 닫기)
		구조체 포인터 student1에 첫 줄 넣고 stdent 인덱스 하나 올리기, 인덱스가 학생 수.
		fscanf(파일포인터, 서식, 변수의주소1, 변수의주소2, …) 쓰기
		나이가 양의 정수인지
		성적이 [0,100]범위인지 확인하고, 아니라면 에러메시지 출력 후 프로그램 종료
		한 개 받을 때마다 확인하자

	insert 하고 또 insert 하면 데이터가 누적해서 추가되도록 하기

2. sort 선택 시

	insert 하지 않았으면 오류 나야 함
	어떻게 확인할 것인가 - 프로그램 처음에 구조체 자료형 만들고 구조체 포인터  *student1 하나 만들기- insert안했다면 포인터는 null일 것
	

	정렬 기준값 선택하도록 함 
	정렬 기준 중 성만 문자 

	선택된 정렬 기준에 맞게 학생 정보를 정렬(오름차순, 구조체를 배열에 넣고 배열 정렬)
	동일 순위면 정렬 순서 상관x

	반복문 이용 정렬된 구조체 출력, 출력되는 내용 중 정렬 기준이 젤 먼저 오도록 하기

3. quit 선택 시

	프로그램 종료

